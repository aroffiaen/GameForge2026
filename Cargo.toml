[package]
name = "GameForge2026"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = "0.18.1"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
ron = "0.10"
